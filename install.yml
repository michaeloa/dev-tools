---
- name: Deploy DEV-TOOLS
  hosts:
    - localhost

  pre_tasks:
    - name: get the username running the deploy
      become: false
      local_action: command whoami
      register: dev_user

    - debug: var=dev_user

#  roles:
#    # Install arcanist on the workstation
#    - role: arcanist_workstation
#      vars:
#        install_path: "{{ devtools_path }}"
#    # Install linters used by arcanist/dev workflow
#    - role: install_linters
#      vars:
#        install_path: "{{ devtools_path }}/linters"
#    # Install utility scripts/applications
#    - role: install_utils
#    # Inspection tests of the install
#    - role: test_install
